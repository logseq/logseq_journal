name: journal_lui_native
description: Native-assets build boundary for the journal LUI OCaml runtime.
publish_to: none
version: 0.0.1

environment:
  sdk: ^3.12.2

dependencies:
  ffi: ^2.1.4
  code_assets: ^1.0.0
  hooks: ^1.0.0
  logging: ^1.3.0
  native_toolchain_c: ^0.17.4

dev_dependencies:
  flutter_lints: ^6.0.0
